# Paper workspace

- Work only in this directory. `/home/t2vg-a100-2/zisu/OpenAgentScaler` is read-only reference material; never edit it or operate its experiments as part of paper writing.
- Write the manuscript in English LaTeX and keep author-facing story/design notes in Chinese where useful.
- Use the unmodified official ICLR 2027 style in `vendor/iclr2027/`. Keep anonymous review mode until explicitly asked otherwise.
- Distinguish maintained implementation, historical run protocols, planned experiments, and measured results. Do not invent results or attribute old scores to a newer method revision.
- Current studies use complete Environment programs in an initially empty persistent library, not the old mandatory World–Scene hierarchy or student-probe gate. The latest method figure has three tiers: a minimal data-centric RSI loop (Researcher → Training data → Model, returning Trajectory + feedback), with short Generate & refine / Train arrow labels but no from-scratch badge or secondary node labels; a typical native-Codex workflow (Analyze trajectories → Generate data → Submit data) with bottlenecks attached to each stage; EnvOpt designs aligned underneath (traceable merge analysis → target-grounded pipeline → target-linked env pool). Keep the caption short and omit API, validation and planner bookkeeping. The editable source is SVG exported to PDF. Do not invent a failed direct-Codex control or guaranteed transfer; distinguish source-to-case queries from retained internal merge-tree provenance.
- The latest framework and 4B studies are in `/home/t2vg-a100-2/zisu/OpenAgentScaler-env-evolution`, also strictly read-only; the feedback worktree is read-only historical material. Paper builds must use the frozen `data/results_20260915.json`, not query live runs. The September 15 (Beijing) update supplements ACE R5–R10 test and R10 dev/training; the original `data/results_20260914.json` remains unchanged. Never operate source experiments or execute generated environment code.
- Keep τ² legacy test-baseline replay incompatibility, native-versus-textual ACE protocol separation, and one-training-seed/three-evaluation-repeat distinctions explicit. ACE has stopped with ten completed dev/test checkpoints; τ²/BFCL retain nine. Do not revive the superseded partial-coverage claim. Choose table checkpoints from dev, not test maxima. Do not erase recorded episode aborts, recovered test-startup failures, or the cancelled ACE R11 attempt from provenance.
- Read `notes/story_zh.md`, `notes/evidence_audit.md`, and `notes/rsi_positioning_zh.md` before materially changing claims. RSIBench-Data is a direct problem-setting reference for bounded cross-model data-centric research relevant to RSI. The Evolver backbone is not retrained in the current method; environment programs and student weights change. Cite EnvHarness, SPADE, RODS, Autodata, Agent-World, FACET, and ACE fairly as method-level precedents.
- Compile with `make` and inspect the rendered PDF after manuscript or figure edits. All caches and tool artifacts must remain in this paper workspace.
- The main evolution plot uses independent x/y ranges, purple primary-split / ochre test lines, small ordinary markers, and a centered pair of split readouts below each benchmark title. Delta numbers are green. Use a compact 2×2 layout (5.5 × 3.7 inches), without Tasks bars, large white rings, or dagger annotations. Preserve the three local studies' data and validation-selection rule; their last plotted rounds are 9/9/10.
- AppWorld comes from `data/appworld_user_20260915.csv`, supplied by the author from another machine, not the locally audited snapshots. Only its TGC main panel is requested: show Base–R8 and omit R9 from both curves and scaling, while preserving the original CSV row. Display Train/Test (not Test-normal); preserve the original test_normal CSV field names. Select R7 from displayed train TGC. Do not add supplementary AppWorld figures, tables, or sections; the previously added appendix figure has been withdrawn and archived. Model/repeats/feedback protocol remain unspecified until supplied. Do not silently merge it into the Qwen3-4B table or invent uncertainty estimates.
